import React from 'react';
import { View, Platform } from 'react-native';

/**
 * Web placeholder for AdMobComponent
 * AdMob is not supported on web - use AdSense instead
 */
const AdMobComponent = () => {
  // AdMob is not available on web platform
  return null;
};

export default AdMobComponent;

