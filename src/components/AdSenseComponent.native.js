import React from 'react';
import { Platform } from 'react-native';

/**
 * Native (Android/iOS) placeholder for AdSenseComponent
 * AdSense is only supported on web platform
 */
const AdSenseComponent = () => {
  // AdSense is not available on native platforms
  return null;
};

export default AdSenseComponent;

